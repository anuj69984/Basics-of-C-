#include <bits/stdc++.h>
using namespace std;

int main(int argc, char const *argv[])
{
	int a,count=0;
	system("color 0A");
	while(cin >> a)
		count++;

	cout <<"total number of int input is : "<<count<<endl;
	return 0;
}